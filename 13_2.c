#define MAX 1000
int main()
{
	int codigo[MAX];
	char nome[MAX][500];
	int dia_nascimento[MAX];
	int mes_nascimento[MAX];
	int ano_nascimento[MAX];
	int sexo[MAX]; // 0 para masculino e 1 para feminino
	return 0;
}
